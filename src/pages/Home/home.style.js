import styled from "styled-components";

const HomeContentContainer = styled.div`
  background-color: #fcfcfc;
  margin: 2em
`

export { HomeContentContainer } 

export default Object.freeze({
  HomeContentContainer
})